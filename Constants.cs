using System;
using System.Collections.Generic;
using System.Text;

namespace Org.Reddragonit.Net.DHCP
{
    internal class Constants
    {
        internal const int DHCP_IN_PORT = 67;
        internal const int DHCP_OUT_PORT = 68;

        internal const string DEFAULT_INTERFACES = "all";
        internal const int DEFAULT_DHCP_LEASE_TIME = 86400;
    }
}
